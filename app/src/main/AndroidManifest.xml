<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team.smart">
    <!-- 통신할때 네트웍 권한 부여하는부분 (꼭 필요하니 추가해야 함) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".activity.SearchMapActivity"></activity>
        <activity android:name=".activity.RentalMapActivity" />
        <activity android:name=".activity.CreateOrderActivity" />
        <activity android:name=".activity.WalletHistoryActivity" />
        <activity android:name=".activity.WalletTransferActivity" />
        <activity android:name=".activity.MyWalletActivity" /> <!-- 액티비티 추가 -->
        <!-- 은지 액티비티 -->
        <activity android:name=".activity.ParkingPaymentActivity" />
        <activity android:name=".activity.ParkingCarSearchActivity" />
        <activity android:name=".activity.ParkingUserCarInfo" />
        <activity android:name=".activity.ParkingMainPageActivity" />
        <activity android:name=".activity.parkingmypageActivity" />
        <activity android:name=".activity.parkingsearchActivity" /> <!-- 지도 api -->
        <!-- 명근 액티비티 -->
        <activity android:name=".activity.SignInActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.RentalContractActivity" />
        <activity android:name=".activity.RentalDetailsActivity" />
        <activity android:name=".activity.RentalListActivity" />
        <activity android:name=".activity.WalletCreateActivity" />
        <activity android:name=".activity.WalletActivity" />
        <activity android:name=".activity.Start" /> <!-- 지혜 액티비티 -->
        <activity android:name=".activity.DetailActivity" />
        <activity android:name=".activity.FoodListActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC6_6DaY0LZLNoSolKCb123A2s1C9Y8mv8" />

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- 세로고정portrait -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>